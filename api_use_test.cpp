#include "renderer.h"

// class Duration {
    
//     static Duration sec(float time) {
//         Duration result;
//         return result;
//     }
// }

void render(Bitmap b)  {

    fillRect(b, 100, 100, 300, 200, 0xFFFFFF00);


    // SET_BACKGROUND(RGB(150, 150, 150));
    // auto p0 = POINT();
    // p0.move(1.0f, 0.0f);
    // p0.show();

    // auto p1 = POINT();
    // p1.moveTo(-1.0f, 1.0f);
    // p0.show();

    // auto p2 = POINT(-1.5f, -1.0f);

    // // p0.animateMove(1.0, 0.0f, Duration::sec(1));
    // p0.animateMove(1.0, 0.0f, SECONDS(1));

    // WAIT(SECONDS(1));

    // auto l0 = LINE(-1.0, 0.0f, 1.0f, 0.0f);
    // auto l1 = LINE(-1.0, 1.0f, 1.0f, 1.0f);

    // PARALLEL(
    //     l0.animateDraw(SECONDS(1)),
    //     l1.animateDrawInverse(SECONDS(2)),
    // );

    // auto c0 = CIRCLE();
    // c0.animateDraw(SECONDS(1));


}
